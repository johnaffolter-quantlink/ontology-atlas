---
description: Build Atlas Current for this project and serve it on this machine
argument-hint: "[--vault <dir>] [--ref <git ref>] [--cdn]"
---

Build Atlas Current for this project with the `atlas-current` skill's steps, passing `$ARGUMENTS`
to `build`. Then start `serve` in the background, give me the local address, and summarise the
forecast audit's flags in one line each. Do not commit `.atlas-current/`.
